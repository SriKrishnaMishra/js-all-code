let index = 0;

 while( index <= 10) {

    console.log(`value of index ${index}`);
    index = index + 2;

 }

 let myArray = ['krishna', 'shiv', 'hanuman','ram']

 console.log(myArray.length);

 let arr = 0;

 while(arr < myArray.length) {

    console.log(`vlaue is ${myArray[arr]}`);
    arr = arr + 1;
 }
